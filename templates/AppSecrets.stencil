struct AppSecrets {
    static let apiKey = "{{ argument.apikey }}"
    static let identityPoolId = "{{ argument.identity_pool_id }}"
    static let platformApplicationArn = "{{ argument.platform_application_arn }}"
    static let topicArn = "{{ argument.topic_arn }}"
}